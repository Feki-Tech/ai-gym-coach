# Optional extras for TALKING to the coach with your microphone
# (push-to-talk speech-to-text, runs locally — any of the app's languages):
#   pip install -r requirements-voice.txt      # or: uv sync --extra voice
# Source of truth: the `voice` extra in pyproject.toml — keep in sync.
sounddevice>=0.4
faster-whisper>=1.0
pycaw>=20230407; sys_platform == 'win32'   # warns when the OS mic volume is too low
